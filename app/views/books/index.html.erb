<%= render 'users/user', users: @users %>
<%= render 'books/book', books: @books %>
<div class="col-md-9">
  <table class="book-index">
  	<thead>
  	  <h2>Books</h2>
  	</thead>
  	<tbody>
  	  <%= @books.each do |book| %>
  	  	<tr>
  	  	  <th>Title</th>
  	  	  <th>Opinion</th>
  	  	</tr>
  	    <tr>
  	      <th><%= book.title %></th>
  	      <th><%= book.opinion %></th>
  	    </tr>
  	  <% end %>
  	</tbody>
  </table>
</div>