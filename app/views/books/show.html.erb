<div class="col-md-9">
  <h1>Book detail</h1>
    <table class="table table-user-show">
  	  <tr>
  	    <th><%= attachment_image_tag @user, :profile_image, :fill, 20, 20, class: "img-circle pull-left profile-thumb", fallback: "no_image.jpg"%></th>
  	    <th><%= link_to user.name, user_path(@user) %></th>
        <th><%= books.title %></th>
      </tr>
  	</table>
</div>